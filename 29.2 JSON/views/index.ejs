<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" id="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" id="beef" name="choice">🥩</button>
    <button type="submit" value="fish" id="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- If no recipe yet, show: -->

    <% if(locals.recipe) { %>
      <h2><%= tacoName %></h2>
    <% }else{ %>
      <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
      <h2>Pick your favourite taco ingredient 👆</h2>
    <% } %>

    <h3>Ingredients:</h3>
    <ul id="ingredientsList">
      <li><%= recipe.protein.name %>, <%= recipe.protein.preparation %></li>
      <li><%= recipe.salsa.name %></li>
      <% recipe.toppings.forEach(salsa => { %>
        <li> <%= salsa.quantity %> of <%= salsa.name %> (<%= salsa.ingredients %>) </li>
      <% }) %>

    </ul>



  </div>
</body>

</html>